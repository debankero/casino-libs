from setuptools import setup, find_packages

setup(
    name='casino_lib',
    version='0.22',
    description='lib',
    author='x0r0x',
    py_modules=['petflip', 'casino'],
    packages=find_packages(),
)
